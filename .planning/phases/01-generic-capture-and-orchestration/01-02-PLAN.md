# Plan 01-02: Report Rendering and Capture Security Tests

## Objective

Extend the existing report surface to present both capture layers and prove that untrusted page/parser/cookie strings remain inert.

## Tasks

1. Replace parser-only summary assumptions with sections for outcome, identity/URL, document, meta, links, viewport/screen, navigation timing, cookies, conditional specialized result, diagnostics, and formatted JSON.
2. Add an explicit cookie-sensitivity warning and distinct messages for no specialized match, specialized success, specialized partial result, and specialized failure.
3. Keep report creation/adoption synchronous and preserve popup/import failure surfaces used by the bookmarklet.
4. Add report tests for generic metadata, cookie values, diagnostics from both layers, conditional specialized sections, complete JSON, and hostile strings in metadata/cookies/parser output.
5. Update the generic success message so it no longer describes only a supported page.

## Must Haves

- No captured string is inserted through `innerHTML`, HTML templates, dynamic attributes, or executable URLs.
- JSON includes generic and specialized results and diagnostics.
- Cookie warning remains visible near the cookie section.

## Verification

Run report security tests plus the complete validation command. Review the renderer diff for DOM construction and `textContent` usage.

## Dependencies

Plan 01-01 contracts and result shape.
