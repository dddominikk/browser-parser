# Plan 04-02: Privacy Transition and Full Smoke Validation

## Objective

Collect final evidence across public/private delivery, authenticated deployment, automated checks, and supported browsers.

## Tasks

1. Confirm the repository-public safety gate: no real gist ID/raw URL/private bookmarklet in public main, logs, or public artifacts; confirm any local override is not committed.
2. After explicit repository-privacy acceptance, run the authenticated deployment smoke test and verify the configured gist file, checksum/content, unrelated-file preservation, and private bookmarklet fetch/Blob/import path.
3. Run all unit/report/build/bookmarklet/deployment tests, typecheck, `npm run validate`, deterministic-build checks, and CI-equivalent bundle smoke.
4. Run current Chrome and Edge manual smoke checks for popup timing, public/private module load, arbitrary-page generic capture, Asana enrichment, report inertness, cookies, and CSP/popup restrictions.
5. Record pass/fail/limitation evidence and update planning status only when every required criterion is either verified or explicitly documented as an external limitation.

## Must Haves

- Secret-gist smoke evidence is authenticated and non-secret.
- Unavailable browser surfaces are limitations, never reported as successful coverage.
- Generated output remains uncommitted.

## Verification

Use the final validation artifact/checklist to prove all v1 requirements map to evidence and that the authoritative brief's completion criteria are satisfied.

## Dependencies

Plans 04-01, 03-02, and the completed bundle/capture phases.
